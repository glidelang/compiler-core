/*
 *  THIS PROGRAM IS DESIGNED TO NOT BE A PROGRAM
 *  IT IS SUPPOSED TO FAIL
 */

import mod::hello::hello; // to-do check import symbols

pub inline func stringFn(s: str) {}
pub unsafe func fn(l: i32) -> u32 {
    return l;
}

// yeah boi
pub func main(l: str) {
    if true {
            let x: i32 = 100;
            let mut y = unsafe {
                fn(34)
            };
            y = 69;
            stringFn("Hello");
            hello();
            unsafe {
                fn("s");
            }
    }
}
