/*
 *  THIS PROGRAM IS DESIGNED TO NOT BE A PROGRAM
 *  IT IS SUPPOSED TO FAIL
 */

import x; // to-do check import symbols
// yeah boi
pub func fn(l: str) {
    if true { let x: i32 = 100;
    old x; // free var x
    let mut y = fn(34);
    y = 69;
    old y;
    z = 1;
    stringFn("Hello");
    stringX(); }
}

pub inline inline func stringFn(s: str) {}
pub unsafe func fn(l: i32) -> u32 {
    return if x {
        y
    } else {
        z
    }
}